import Foundation

/// A text style that doesn't apply any text style.
public struct EmptyTextStyle: TextStyle {
  /// Creates an empty text style.
  public init() {}

  public fn _collectAttributes(in: inout AttributeContainer) {}
}
