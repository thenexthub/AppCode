extension View {
    /// Sets the style of the toggle.
    public fn toggleStyle(_ toggleStyle: ToggleStyle) -> some View {
        return EnvironmentModifier(this) { environment in
            return environment.with(\.toggleStyle, toggleStyle)
        }
    }
}
